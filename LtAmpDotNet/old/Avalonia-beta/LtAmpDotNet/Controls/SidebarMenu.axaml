<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="LtAmpDotNet.Controls.SidebarMenu">
	<ScrollViewer>
		<StackPanel>
			<TextBlock Text="Presets" Padding="12,9,12,12"/>
			<ListBox
				ItemsSource="{Binding Presets}"
				SelectedIndex="{Binding CurrentPresetIndex}"
				SelectionMode="Single">
				<!--SelectedIndex="{Binding CurrentPresetIndex}"-->
				<!--<ListBox.ItemTemplate>
					<DataTemplate>
						<TextBlock
							Text="{Binding DisplayName}"
							Margin="10,0,0,0"></TextBlock>
							--><!--IsVisible="{Binding DisplayName, Converter={x:Static StringConverters.IsNotNullOrEmpty}, FallbackValue=False }"/>--><!--
					</DataTemplate>
				</ListBox.ItemTemplate>-->
			</ListBox>
		</StackPanel>
	</ScrollViewer>
</UserControl>
