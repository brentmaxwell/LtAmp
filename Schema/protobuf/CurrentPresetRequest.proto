syntax = "proto2";

/*
 * Requests the current preset from the amp
 *
 * response: CurrentPresetStatus message
 */
message CurrentPresetRequest {
    // Always true on requests
    required bool request = 1;
}
