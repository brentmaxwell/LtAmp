syntax = "proto2";
option csharp_namespace = "LtAmpDotNet.Lib.Models.Protobuf";

/*
 * The firmware version of the amp
 */
message FirmwareVersionStatus {
    // Firmware Version
    required string version = 1;
}
