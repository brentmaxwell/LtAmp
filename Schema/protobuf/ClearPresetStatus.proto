syntax = "proto2";
option csharp_namespace = "LtAmpDotNet.Lib.Models.Protobuf";

/*
 * Response to a ClearPreset message with the slot index cleared
 */
message ClearPresetStatus {
    // slot index cleared
    required int32 slotIndex = 1;
}
